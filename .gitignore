# Environments
.env
.venv
env/
venv/
ENV/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache

# OS Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs/
*.log

# Data & Models (if they are too big, we keep active ones in backend/ml/ though)
# We will not ignore backend/ml/*.pkl because they seem to be required for the app to run out-of-the-box,
# unless they are very big and we should use LFS. The largest is ~50MB, so it's under GitHub's 100MB limit.

# PCAPs
pcaps/*.pcap
pcaps/*.pcapng

# Frontend build output
frontend/dist/

# Frontend backups
frontend/src_backup_*/

# ML sample data & temp outputs
ml/data/
ml/evaluation_results/stress_test_report.json
